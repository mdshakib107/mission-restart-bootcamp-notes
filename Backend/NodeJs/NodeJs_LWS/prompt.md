আমি একটি Node.js tutorial video-এর transcript upload করছি।

তুমি এটিকে summary বা short notes বানাবে না। Instructor-এর পুরো teaching flow, explanation, example, analogy, code walkthrough, warning, recap, interview question এবং task বজায় রেখে বাংলায় বইয়ের একটি পূর্ণাঙ্গ chapter-এর মতো লিখবে।

নির্দেশনা:

1. Transcript-এর শুরু থেকে শেষ পর্যন্ত একই sequence বজায় রাখবে।
2. গুরুত্বপূর্ণ কোনো বক্তব্য বাদ দেবে না বা অপ্রয়োজনীয়ভাবে সংক্ষিপ্ত করবে না।
3. ভাষা হবে সহজ, স্বাভাবিক, beginner-friendly এবং বইয়ের মতো বাংলা।
4. `Node.js`, `JavaScript`, `module`, `npm`, `package.json`, `event loop`, `callback`, `Promise`, `async/await`, `server`, `request`, `response`, `middleware`, `API`, `stream`, `Buffer` ইত্যাদি technical term English-এই রাখবে।
5. সব code proper fenced code block-এ লিখবে।
6. Code-এর আগে ও পরে Instructor-এর reasoning step-by-step বাংলায় বুঝিয়ে লিখবে।
7. Expected output থাকলে output এবং কেন সেই output হবে—দুটিই লিখবে।
8. Instructor-এর analogy, example, question-answer flow এবং repetition বজায় রাখবে।
9. Common mistake, tricky case এবং interview question থাকলে আলাদা subsection করবে।
10. Lecture recap আলাদা section-এ রাখবে।
11. Assignment বা task থাকলে শেষে `Assignment / Task` section যোগ করবে।
12. নিজের নতুন concept, analogy, example বা code যোগ করবে না।
13. Technical inconsistency থাকলে মূল বক্তব্য পরিবর্তন না করে আলাদা `Technical Note` দিতে পারো।
14. Timestamp বাদ দিতে পারো, কিন্তু বক্তব্যের order পরিবর্তন করবে না।
15. Output Markdown `.md` format-এ হবে।
16. শেষে একটি concise `Final Recap` থাকবে।
17. পুরো কাজ শেষে downloadable `.md` file তৈরি করবে।

সবচেয়ে গুরুত্বপূর্ণ:

এটি summary নয়। এটি transcript-এর সম্পূর্ণ বক্তব্য ও teaching flow বজায় রেখে তৈরি করা একটি পূর্ণাঙ্গ বাংলা Node.js book chapter।
