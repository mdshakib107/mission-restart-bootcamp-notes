# JavaScript Day 07 — Projects Complete A to Z Notes

Source Transcript: :contentReference[oaicite:0]{index=0}

---

# 1. Introduction

আজকের দিন:

```text
Project Day
```

এই দিনটিতে:

- Previous concepts apply করা হয়েছে
- Real project building দেখানো হয়েছে
- Logic building শেখানো হয়েছে
- Problem solving approach শেখানো হয়েছে

---

# 2. Projects Covered

আজ দুইটি project নিয়ে আলোচনা হয়েছে:

1. Rock Paper Scissors Game
2. Secret Number Guessing Game

---

# 3. Important Message

Instructor বলেছেন:

```text id="ndaq7f"
Coding শুধু syntax না, problem solving mindset important
```

---

# 4. Main Goal

আগের ৬ দিনের concepts ব্যবহার করা হয়েছে:

- Variables
- Conditionals
- Functions
- Loops
- Prompt
- Random numbers
- Recursion
- Logic building

---

# 5. Project Building Approach

Direct coding শুরু করা উচিত না।

আগে করতে হবে:

```text id="1ub8g8"
Assumption and logical breakdown
```

---

# 6. First Project

```text
Rock Paper Scissors Game
```

---

# 7. Game Players

Game খেলবে:

- Human/User
- Computer

---

# 8. User Choices

User choose করতে পারবে:

- Rock
- Paper
- Scissors

---

# 9. Computer Choice

Computer randomly choose করবে:

- Rock
- Paper
- Scissors

---

# 10. Winning Rules

| Rule                 | Winner   |
| -------------------- | -------- |
| Rock breaks Scissors | Rock     |
| Paper wraps Rock     | Paper    |
| Scissors cuts Paper  | Scissors |

---

# 11. Project Assumptions

Coding শুরু করার আগে assumptions লেখা হয়েছে।

---

# 12. Assumption 1

```text id="b4ghmu"
User input নিতে হবে
```

---

# 13. Assumption 2

```text id="n4dqrz"
Computer choice random হবে
```

---

# 14. Assumption 3

```text id="hcc7e5"
User choice এবং computer choice compare করতে হবে
```

---

# 15. Assumption 4

```text id="l5ts7v"
Winner announce করতে হবে
```

---

# 16. Optional Assumption

```text id="3szxj4"
Play again option থাকতে পারে
```

---

# 17. Why Assumptions Important?

কারণ:

```text id="tqqnn9"
Coding এর আগে logical roadmap তৈরি হয়
```

---

# 18. Project Structure

সব code function এর ভিতরে রাখা হয়েছে।

---

# 19. Function Example

```js id="b3x47o"
function rockPaperScissorsGame() {}
```

---

# 20. Important Reminder

Function declare করলে execute হয় না।

Call করতে হয়।

---

# 21. Calling Function

```js id="7js2r5"
rockPaperScissorsGame();
```

---

# 22. Initial Testing

```js id="xtl9j7"
console.log("Getting started");
```

---

# 23. HTML Linking Mentioned

Project structure:

- index.html
- RPS folder
- rps.js

---

# 24. Anchor Tag Mention

HTML এ:

```html id="zffo1m"
<a href=""></a>
```

ব্যবহার করে page linking করা হয়েছে।

---

# 25. User Input

Browser prompt ব্যবহার করা হয়েছে।

---

# 26. Prompt Method

```js id="i07ku1"
prompt();
```

---

# 27. Browser Global Method

Instructor বলেছেন:

```text id="k3omj0"
prompt এবং alert browser global methods
```

---

# 28. Taking User Input

```js id="btz3xx"
const userChoicePrompt = prompt("Enter rock paper or scissors");
```

---

# 29. Why Lowercase Needed?

কারণ:

```text id="ozbndm"
ROCK এবং rock equal না
```

---

# 30. String Comparison Important

JavaScript string comparison:

```text id="lmt40l"
Case-sensitive
```

---

# 31. Lowercase Conversion

```js id="rbx24w"
toLowerCase();
```

---

# 32. Example

```js id="m1ghw8"
const userChoice = userChoicePrompt.toLowerCase();
```

---

# 33. Computer Choice Variable

```js id="cz3m8r"
let computerChoice;
```

---

# 34. Random Number Generation

Important topic।

---

# 35. Random Method

```js id="2s2trf"
Math.random();
```

---

# 36. Important Concept

```text id="35nm5y"
Math.random() returns decimal value
```

---

# 37. Need

Need random number between:

```text
1 to 3
```

---

# 38. Scaling Random Number

Formula explained।

---

# 39. Floor Method

```js id="n4z7to"
Math.floor();
```

---

# 40. Final Formula

```js id="91mzk5"
Math.floor(Math.random() * 3) + 1;
```

---

# 41. Why +1 Needed?

কারণ:

```text id="2nspop"
Math.random() starts from 0 range
```

---

# 42. Random Mapping

| Number | Choice   |
| ------ | -------- |
| 1      | Rock     |
| 2      | Paper    |
| 3      | Scissors |

---

# 43. Condition Example

```js id="10z0xg"
if (randomNumber === 1) {
  computerChoice = "rock";
}
```

---

# 44. if-else Used

Instructor বলেছেন:

```text id="8e5vrm"
switch-case দিয়েও করা যেত
```

---

# 45. Variable Declaration Discussion

| Variable | Why           |
| -------- | ------------- |
| const    | reassigned না |
| let      | reassigned হয় |

---

# 46. User Choice Print

```js id="vv75dy"
console.log(userChoice);
```

---

# 47. Computer Choice Print

```js id="s76t6l"
console.log(computerChoice);
```

---

# 48. Important Practice Advice

```text id="y2s7zy"
Step by step testing করতে হবে
```

---

# 49. Winner Logic

User wins if:

- Rock vs Scissors
- Paper vs Rock
- Scissors vs Paper

---

# 50. User Win Condition

```js id="pdg2q9"
if(
(userChoice === "rock" && computerChoice === "scissors")
)
```

---

# 51. OR Operator

Multiple conditions join করতে:

```js id="r6b6zy"
||
```

---

# 52. Game Tie Logic

```js id="97n01d"
userChoice === computerChoice;
```

---

# 53. Tie Output

```text id="ljr4ib"
Game is tie
```

---

# 54. Computer Win Logic

Opposite conditions।

---

# 55. Invalid Input Handling

If input invalid হয়:

```text id="g6r0fg"
Please check the input
```

---

# 56. Full Game Flow

```text
Take user input
↓
Generate random computer choice
↓
Compare
↓
Declare winner
```

---

# 57. Play Again Feature

Prompt again ব্যবহার করা হয়েছে।

---

# 58. Prompt Example

```js id="71uhzx"
prompt("Do you want to play again?");
```

---

# 59. Important Browser Behavior

Cancel button click করলে:

```text id="xj4a9l"
prompt returns null
```

---

# 60. Null Handling

Fallback value ব্যবহার করা হয়েছে।

---

# 61. Example

```js id="7k8s9l"
const playAgain = playAgainPrompt ? playAgainPrompt.toLowerCase() : "no";
```

---

# 62. Ternary Operator Used

```js id="h4r6a5"
condition ? trueValue : falseValue;
```

---

# 63. Play Again Logic

```js id="o0dcbq"
if(playAgain === "yes")
```

---

# 64. Recursion Used

Same function আবার call করা হয়েছে।

---

# 65. Recursive Call

```js id="z1h6g3"
rockPaperScissorsGame();
```

---

# 66. Exit Case

```js id="10i7n8"
console.log("Thanks for playing");
```

---

# 67. Important Learning

আগের lesson এর recursion এখানে practical use হয়েছে।

---

# 68. Main Concepts Used

| Concept        | Usage           |
| -------------- | --------------- |
| Function       | Game wrapper    |
| Condition      | Winner logic    |
| Random         | Computer choice |
| Prompt         | User input      |
| Recursion      | Replay          |
| String methods | lowercase       |

---

# 69. Important Advice

```text id="jlwmji"
Copy-paste bad না যদি বুঝে করা হয়
```

---

# 70. Second Project

```text
Secret Number Guessing Game
```

---

# 71. Project Idea

User number guess করবে।

---

# 72. Number Range

```text
1 to 10
```

---

# 73. Random Secret Number

Computer secretly random number generate করবে।

---

# 74. User Guess

User repeatedly guess করবে।

---

# 75. Hint System

Program বলবে:

- Too low
- Too high

---

# 76. Winning Condition

Correct guess হলে:

```text id="l7iqcl"
Congrats
```

---

# 77. Attempts Count

Program count করবে:

```text id="pnjz0q"
How many attempts user took
```

---

# 78. Example Output Mentioned

```text
Too high
Too low
Correct guess
```

---

# 79. Important Hint

Previous project এর logic reuse করা যাবে।

---

# 80. Suggested Variables

Instructor suggested:

```text
min number
max number
secret number
guess
attempts
```

---

# 81. Secret Number Formula

Random formula reuse হবে।

---

# 82. Loop Needed

Instructor বলেছেন:

```text id="zq5c8v"
Loop ব্যবহার করতে হবে
```

---

# 83. Loop Purpose

কারণ:

```text id="cokl2m"
User repeatedly guess করবে
```

---

# 84. Guess Comparison

Need:

```text
guess < secret
guess > secret
guess === secret
```

---

# 85. Low Hint

If guess low:

```text id="l7vqkk"
Too low
```

---

# 86. High Hint

If guess high:

```text id="x5zt3f"
Too high
```

---

# 87. Replay Option

Again:

```text id="1pzt0l"
Play again feature রাখা যাবে
```

---

# 88. Instructor Advice

```text id="fc83lb"
নিজে চেষ্টা না করে code দেখে ফেলো না
```

---

# 89. Recommended Learning Process

1. Try yourself
2. Fail
3. Retry
4. Take break
5. Retry again
6. Then only check hints

---

# 90. Upcoming Topics

Next module topics:

- Hoisting
- Scope
- Execution Context
- this keyword

---

# 91. Scope Importance

Instructor বলেছেন:

```text id="aqck0q"
Scope JavaScript এর সবচেয়ে fundamental concept গুলোর একটি
```

---

# 92. Module Completion

Day 07 দিয়ে:

```text id="y3k8dc"
Module 1 complete
```

---

# 93. Main Lessons from Day 07

আজ শেখানো হয়েছে:

- Real project building
- Problem decomposition
- Assumption writing
- Random generation
- Browser prompt
- Recursive replay system
- Game logic
- User interaction
- Debugging mindset

---

# 94. Important Development Mindset

```text id="2h6m9x"
Project building আগে চিন্তা → পরে code
```

---

# 95. Final Advice

Instructor repeatedly বলেছেন:

```text id="pm7hvn"
শুধু ভিডিও দেখে যেও না, code করো
```

---

# End of Day 07 Notes

```

```

নিচে Day 07-এর দুইটা project code দিলাম।

```js
// Project 1: Rock Paper Scissors Game

function rockPaperScissorsGame() {
  const userChoicePrompt = prompt("Enter rock, paper, or scissors");

  const userChoice = userChoicePrompt ? userChoicePrompt.toLowerCase() : "";

  let computerChoice;

  const randomNumber = Math.floor(Math.random() * 3) + 1;

  if (randomNumber === 1) {
    computerChoice = "rock";
  } else if (randomNumber === 2) {
    computerChoice = "paper";
  } else {
    computerChoice = "scissors";
  }

  console.log("User selected:", userChoice);
  console.log("Computer selected:", computerChoice);

  if (
    (userChoice === "rock" && computerChoice === "scissors") ||
    (userChoice === "paper" && computerChoice === "rock") ||
    (userChoice === "scissors" && computerChoice === "paper")
  ) {
    console.log("You win!");
  } else if (userChoice === computerChoice) {
    console.log("Game is tie!");
  } else if (
    (userChoice === "rock" && computerChoice === "paper") ||
    (userChoice === "paper" && computerChoice === "scissors") ||
    (userChoice === "scissors" && computerChoice === "rock")
  ) {
    console.log("Computer wins!");
  } else {
    console.log("Please check your input. Use rock, paper, or scissors.");
  }

  const playAgainPrompt = prompt("Do you want to play again? yes or no");

  const playAgain = playAgainPrompt ? playAgainPrompt.toLowerCase() : "no";

  if (playAgain === "yes") {
    rockPaperScissorsGame();
  } else {
    console.log("Thanks for playing. See you next time!");
  }
}

rockPaperScissorsGame();
```

```js
// Project 2: Secret Number Guessing Game

function secretNumberGuessingGame() {
  const minNumber = 1;
  const maxNumber = 10;

  const secretNumber = Math.floor(Math.random() * maxNumber) + minNumber;

  let attempts = 0;
  let guessedCorrectly = false;

  while (!guessedCorrectly) {
    const userGuessPrompt = prompt(
      `Guess a number between ${minNumber} and ${maxNumber}`,
    );

    if (userGuessPrompt === null) {
      console.log("Game cancelled.");
      break;
    }

    const userGuess = Number(userGuessPrompt);
    attempts++;

    if (userGuess < secretNumber) {
      console.log("Too low. Try again.");
    } else if (userGuess > secretNumber) {
      console.log("Too high. Try again.");
    } else if (userGuess === secretNumber) {
      console.log(`Congrats! You guessed the number in ${attempts} attempts.`);
      guessedCorrectly = true;
    } else {
      console.log("Invalid input. Please enter a number.");
    }
  }

  const playAgainPrompt = prompt("Do you want to play again? yes or no");

  const playAgain = playAgainPrompt ? playAgainPrompt.toLowerCase() : "no";

  if (playAgain === "yes") {
    secretNumberGuessingGame();
  } else {
    console.log("Thanks for playing. See you next time!");
  }
}

secretNumberGuessingGame();
```
