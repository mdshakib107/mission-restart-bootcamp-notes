# ES6 Function Default Parameter (=) — Complete Practice Question Set

## 📌 Context

এই practice set টা **ES6 default parameter** concept পুরোপুরি **practice করে শেখার জন্য** বানানো।  
লক্ষ্য: function call করার সময় argument না দিলে কীভাবে safe behavior নিশ্চিত করা যায়।

Beginner → Core → Intermediate → Advanced ধাপে ধাপে যাবে।

---

## 1️⃣ Concept Analysis

### Core Concepts to Learn

1. **Default Parameter কী**
   - Function parameter এ `=` দিয়ে default value দেওয়া

2. **Undefined vs Default Value**
   - Argument না পাঠালে parameter হয় `undefined`
   - Default থাকলে সেটা replace হয়

3. **NaN কেন হয়**
   - Undefined দিয়ে arithmetic করলে `NaN`

4. **Operation-based Default Rules**
   - Add / Subtract → `0`
   - Multiply → `1`
   - String → `''`
   - Array → `[]`
   - Object → `{}`

5. **Partial Argument Passing**
   - কিছু argument দিলে, বাকিগুলো default নেয়

---

## 2️⃣ Code Analysis (Given Code)

```js
function sum(num1 = 0, num2 = 0) {
  const total = num1 + num2;
  console.log(num1, num2, total);
}
```

- Argument না দিলে `0 + 0`
- NaN avoid করা

---

```js
function multiply(num1 = 1, num2 = 1) {
  const result = num1 * num2;
  console.log(result);
}
multiply();
```

- Multiply এর জন্য default `1`
- Empty call হলেও logical output

---

```js
function fullName(first, last = "") {
  const name = first + "" + last;
  console.log(name);
}
fullName("kamruzzaman");
```

- Optional parameter handle
- String default usage

---

## 3️⃣ Practice Question Set

---

## 🟢 A. Beginner Practice

### Q1

নিচের code চালালে কী output হবে?

```js
function add(a = 0, b = 0) {
  console.log(a + b);
}
add(5);
```

**Think about:**

- Missing argument
- Default value

**Concept:**

- Default parameter basics

---

### Q2

এই function এ default না থাকলে কী problem হতো?

```js
function sum(x, y) {
  console.log(x + y);
}
sum(10);
```

**Think about:**

- `undefined + number`

**Concept:**

- NaN reason

---

### Q3

নিচের code এর output predict করো:

```js
function multiply(x = 1, y = 1) {
  console.log(x * y);
}
multiply(5);
```

**Think about:**

- Default second parameter

**Concept:**

- Partial argument passing

---

### Q4

এই function টায় default value বসাও যাতে error না হয়:

```js
function greet(name) {
  console.log("Hello " + name);
}
greet();
```

**Think about:**

- String default

**Concept:**

- Safe function design

---

## 🟡 B. Core Practice

### Q5

এই function rewrite করো default parameter ব্যবহার করে:

```js
function discount(price, rate) {
  return price - price * rate;
}
```

👉 এমন করো যেন `rate` না দিলে discount না হয়।

**Think about:**

- Subtraction rule

**Concept:**

- Logical default value

---

### Q6

একটা function বানাও:

- 3টা parameter
- যেকোনো একটা missing হলে function crash করবে না

**Think about:**

- All parameters default

**Concept:**

- Defensive coding

---

### Q7

এই code টা কেন bad design explain করো:

```js
function multiply(a, b) {
  b = b || 1;
  return a * b;
}
```

**Think about:**

- ES6 alternative

**Concept:**

- Default parameter vs logical OR

---

## 🟠 C. Intermediate Practice

### Q8

একটা shopping cart total calculator বানাও:

- items array parameter
- default value এমন হবে যেন empty call এ error না হয়

**Think about:**

- Array default `[]`

**Concept:**

- Array default parameter

---

### Q9

User profile formatter বানাও:

- name required
- role optional
- role না দিলে empty string

**Think about:**

- Optional parameter design

**Concept:**

- Real-world function API

---

### Q10

এই function call গুলোর output predict করো:

```js
function test(a = 5, b = 10) {
  console.log(a, b);
}

test();
test(20);
test(undefined, 30);
```

**Think about:**

- Explicit `undefined`

**Concept:**

- Default trigger rule

---

## 🔴 D. Advanced / Interview Practice

### Q11

Default parameter আর function body এর ভিতরে assignment এর পার্থক্য লেখো।

```js
function demo(a = 10) {}
```

vs

```js
function demo(a) {
  a = a || 10;
}
```

**Think about:**

- Edge cases
- `0`, `false`

**Concept:**

- Robust design

---

### Q12

Performance-wise default parameter কি function call এ extra cost যোগ করে?
👉 তোমার reasoning লেখো।

**Think about:**

- Execution time
- Readability vs performance

**Concept:**

- Engineering judgment

---

### Q13 (Interview-style)

একটা rule-set লেখো:

> কীভাবে default value decide করবে production code এ?

**Think about:**

- Business logic
- Predictable behavior

**Concept:**

- API design thinking

---

## 4️⃣ Thumb Rule Summary (From Code)

- Add / Subtract → `0`
- Multiply → `1`
- String → `''`
- Array → `[]`
- Object → `{}`

---

## 5️⃣ Coverage Check

- [x] Default parameter syntax
- [x] NaN prevention
- [x] Partial arguments
- [x] Real-world use cases
- [x] Interview depth

---
